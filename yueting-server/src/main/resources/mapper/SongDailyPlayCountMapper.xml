<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiangce.yueting.mapper.SongDailyPlayCountMapper">

    <insert id="insertOrUpdate">
        INSERT INTO song_daily_play_count (song_id, play_date, play_count)
        VALUES (#{songId}, #{playDate}, #{playCount})
        ON DUPLICATE KEY UPDATE play_count = play_count + #{playCount}
    </insert>

</mapper>
