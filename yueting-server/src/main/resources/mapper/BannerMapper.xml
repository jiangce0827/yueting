<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiangce.yueting.mapper.BannerMapper">

    <select id="pageBanners" resultType="com.jiangce.yueting.domain.po.Banner">
        SELECT banner_id, title, image_url, target_type, target_id, target_url, sort_order, status, created_at, updated_at
        FROM banner
        ORDER BY sort_order ASC, banner_id DESC
    </select>

    <select id="selectEnabledBanners" resultType="com.jiangce.yueting.domain.vo.BannerVO">
        SELECT banner_id, title, image_url, target_type, target_id, target_url
        FROM banner
        WHERE status = 1
        ORDER BY sort_order ASC, banner_id DESC
    </select>
</mapper>