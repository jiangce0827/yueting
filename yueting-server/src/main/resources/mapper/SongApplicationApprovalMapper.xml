<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiangce.yueting.mapper.SongApplicationApprovalMapper">


    <insert id="insertBatch">
        insert into song_application_approval(approval_id, application_id, artist_id, notified_at, responded_at, status) values
        <foreach collection="list" item="item" separator=",">
            (#{item.approvalId}, #{item.applicationId}, #{item.artistId}, #{item.notifiedAt}, #{item.respondedAt}, #{item.status})
        </foreach>
    </insert>
</mapper>